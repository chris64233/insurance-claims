spring:
  application:
    name: insurance-claims
  datasource:
    url: jdbc:h2:mem:insurance_claims;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

server:
  port: 8080
